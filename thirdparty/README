This directory contains NSIS plugins needed to build the OTRS Windows Installer.
They are stored in this directory so OTRS can still be built if the original 
source (which is the NSIS wiki) would ever go away.